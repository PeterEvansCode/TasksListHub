<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TaskListHub" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:background">@android:color/background_dark</item>  <!-- Default Background for Dark Mode -->
        <item name="colorSurface">@android:color/background_dark</item>  <!-- Default Surface for Dark Mode -->
        <item name="colorOnBackground">@android:color/primary_text_dark</item>  <!-- Default Text Color for Dark Mode -->
        <item name="colorOnSurface">@android:color/primary_text_dark</item>  <!-- Default Text Color for Dark Mode -->
    </style>
</resources>
